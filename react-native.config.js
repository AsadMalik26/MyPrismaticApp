module.export = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./assets/fonts/'],
};
// the above method use command npx react-native link
// the above method is obsolete since react-native version 0.60

// for latest version of react-native is: $ npx react-native-asset -a ./assets/fonts
